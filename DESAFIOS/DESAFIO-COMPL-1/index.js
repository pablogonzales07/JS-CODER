//PRIMER DESAFIO COMPLEMENTARIO:

//EJEMPLO 1:


/* let entreno = prompt("usted entrena?, consteste si o no");

while(entreno === "si"){

    let diasDeEntreno = parseInt(prompt("cuantos dias a la semana"));
 
    if(diasDeEntreno < 3){
        alert("no estas entrenando lo necesario");
    }
    else if((diasDeEntreno >=3) && (diasDeEntreno <=5)){
        alert("estras entrenando lo recomendado");
    }
    else if((diasDeEntreno > 5) && (diasDeEntreno <=7)){
        alert("sos un ANIMAL");
    }
    else if(Number.isNaN(diasDeEntreno)){
        alert("el valor ingresado es una letra, por favor ingrese un numero");
    }
    else{
        alert("esta ingresando un numero incorrecto");
    }

    entreno = prompt("Usted hace actividad fisica, ingrese si o no");              
}

alert("deberias arrancar actividad fisica");
 */

//EJEMPLO 2:

/* let entrenamiento = prompt("ingrese cuantos dias a la semana entrena, ingrese un valor numerico por favor");

while(entrenamiento != "ESC"){
    if(entrenamiento === 0){
        alert("Deberias arrancar a entrenar");
    }
    else if(entrenamiento < 3){
        alert("no estas entrenando lo recomendado");
    }
    else if((entrenamiento >= 3) && (entrenamiento <= 5)){
        alert("estas entrenando lo recomendado");
    }
    else if((entrenamiento > 5) && (entrenamiento <= 7)){
        alert("SOS UN ANIMAL");
    }
    else{
        alert("estas ingresando un valor incorrecto");
    }

    entrenamiento = prompt("ingrese cuantos dias a la semana enrena, ingrese un valor numerico por favor");
    
}

alert("terminaste el bucle"); */
